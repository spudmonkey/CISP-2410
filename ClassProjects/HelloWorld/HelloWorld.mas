	CLEAR
	STORE 		INDEX
WHILE,	LOAD		STR_BASE
	ADD		INDEX
	STORE		ADDR
	CLEAR
	ADDI		ADDR
	SKIPCOND 400
	JUMP 		DO
	JUMP 		END_WHILE
DO,	OUTPUT
	LOAD 		INDEX
	ADD 		ONE
	STORE 		INDEX
	JUMP 		WHILE
	END_WHILE,	HALT
ONE, 	 DEC 1
INDEX, 	 DEC 0
ADDR,	 HEX 0
STR_BASE, HEX 14
STR,	 DEC 72		/H
	 DEC 69		/E
	 DEC 76		/L
	 DEC 76		/L
	 DEC 79		/O
	 DEC 13		/carriage return
	 DEC 87		/W
	 DEC 79		/O
	 DEC 82		/R
	 DEC 76		/L
	 DEC 68		/D
NULL, 	 DEC 0		/NULL CHAR




